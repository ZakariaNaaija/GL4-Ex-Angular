import { CielDirective } from './ciel.directive';

describe('CielDirective', () => {
  it('should create an instance', () => {
    const directive = new CielDirective();
    expect(directive).toBeTruthy();
  });
});
